# Optional. Used for AI commentary on backtest results. Without it the app still runs, commentary just falls back to a deterministic summary.
GROQ_API_KEY=

# Optional. Backup market data provider. Free tier is 500 requests per day.
ALPHA_VANTAGE_API_KEY=

# Optional. Override the public site URL used for sitemap and metadata.
NEXT_PUBLIC_SITE_URL=
